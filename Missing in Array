class Solution {
    int missingNum(int arr[]) {
        int n = arr.length + 1;
        int XOR = 0;
        for (int i = 0; i <= n; i++){
            XOR ^= i;
        }
        for (int num : arr){
            XOR ^= num;
        }
        return XOR;
        
    }
}
