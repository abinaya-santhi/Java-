class Solution {
    public int reverseDigits(int n) {
        // Code here
        int temp = n;
        int reverse = 0;
        int result;
        while (temp != 0){
            result = temp % 10;
            temp  = temp / 10;
            reverse = reverse * 10 + result;
        }
        return reverse;
    }
}
